# React/Redux Blog Example

- Add required libs:

  ```
  npm install --save redux react-redux axios redux-thunk
  ```

  ```
  npm install --save lodash
  ```

- Check Libs
  - `npm audit`
  - `npm audit fix`




